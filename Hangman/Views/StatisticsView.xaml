<UserControl x:Class="Hangman.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#FFD0D0" Height="Auto" Width="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="User Stats" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding StatsList}" AutoGenerateColumns="False" IsReadOnly="True" Margin="20" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="User Name" Binding="{Binding UserName}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Games Played" Binding="{Binding Played}" Width="*"/>
                <DataGridTextColumn Header="Games Won" Binding="{Binding Won}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Back" Command="{Binding BackCommand}" Width="150" Height="35" Margin="20" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>